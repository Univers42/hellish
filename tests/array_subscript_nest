a=(x y z); b=(p); echo "${a[${#b[@]}]}"
a=(x y z); echo "${a[$((${#a[@]} - 1))]}"
a=(x y z); echo "${a[$((${#a[@]}-2))]}"
a=(x y z); echo ${a[${#a[@]}-1]}
a=(x y z); i=(1); echo "${a[${i[0]}]}"
a=(x y z); i=(0 2); echo "${a[${i[1]}]}"
a=(x y z); i=(0 2); echo "${a[${i[1]}]:-none}"
a=(x y z); b=(p q); echo "${a[${#b[@]}]:-none}"
a=(x y z); b=(p q); echo "${a[$((${#b[@]}+5))]:-none}"
a=(x y z); echo "${a[$((${#a[@]}-1))]#z}"
a=(x y z); echo "${a[$((${#a[@]}-1))]/z/Z}"
a=(x y z); echo "${a[$(( ${#a[@]} - 1 ))]}"
a=(x y z); echo "${a[$(echo ${#a[@]})-1]}"
declare -A m=([k]=v [kk]=vv); k=(k kk); echo "${m[${k[1]}]}"
declare -A m=([k]=v); k=(k); echo "${m[${k[0]}]:-none}"
a=(x y z); echo "${a[@]:${#a[@]}-1}"
a=(x y z); echo "${a[@]:$((1+1))}"
a=(x y z); s=ab; echo "${a[@]:1:${#s}}"
a=(x y z); n=1; echo "${a[@]:n}"
a=(x y z); n=1; echo "${a[@]:$n:1}"
a=(x y z); echo "${a[@]: -1}"
a=(x y z); echo "${a[@]:${#a[@]}-2}"
a=(x y z); b=(p q); echo "${a[*]:${#b[@]}}"
a=(x y z); echo "${a[@]:$((${#a[@]}-1)):1}"
a=(x y z); i=(0 1 2); echo "${a[@]:${i[1]}:${#i[@]}}"
a=(x y z); echo "${#a[$((${#a[@]}-1))]}"
a=(1 2 3); echo $(( a[${#a[@]}-1] + 1 ))
a=(1 2 3); echo $(( ${a[$((${#a[@]}-1))]} * 2 ))
a=(x y z); for ((i=0; i<${#a[@]}; i++)); do printf '%s' "${a[$((${#a[@]}-1-i))]}"; done; echo
a=(x y z); echo "${a[${#a[@]}]:-past-the-end}"
a=(1 2 3); echo $(( a[2] ))
a=(1 2 3); i=1; echo $(( a[$i] )) $(( a[i] )) $(( a[i+1] ))
a=(1 2 3); echo $(( a[${#a[@]}-1] )) $(( a[a[0]] )) $(( a[-1] ))
a=(1 2 3); (( a[1] = 9 )); echo "${a[@]}"
a=(1 2 3); (( a[0]++ )); (( a[2] += 10 )); (( --a[1] )); echo "${a[@]}"
a=(1 2 3); s=0; for ((i=0;i<${#a[@]};i++)); do (( s += a[i] )); done; echo $s
a=(5 6 7); s=0; for i in "${!a[@]}"; do (( s += a[i] * i )); done; echo $s
declare -A m=([k]=5); echo $(( m[k] + 1 )); (( m[k]++ )); echo ${m[k]}
x=5; echo $(( x[0] )); (( x[2] = 7 )); echo "${x[@]}"
a=(1 2 3); echo $(( a[5] + 1 ))
a=(1 2 3); echo $(( a[1] ? a[0] : a[2] ))
a=(2 3); echo $(( a[0] ** a[1] ))
a=(1 2 3); echo $(( ${a[a[0]]} + a[a[1]] ))
a=(x y z); i=7; echo "${a[$((i % ${#a[@]}))]}"
a=(x y z); for i in 0 1 2 3 4 5; do printf '%s' "${a[$((i % ${#a[@]}))]}"; done; echo
a=(x y z); i=4; echo "${a[i % ${#a[@]}]}" "${a[$i % ${#a[@]}]}" "${a[$((i % 3))]}"
a=(x y z); i=5; echo "${a[$(( (i + 1) % ${#a[@]} ))]}" "${a[$((${#a[@]} - i % ${#a[@]}))]}"
a=(x y z); i=1; a[$((i % ${#a[@]}))]=Q; echo "${a[@]}"
a=(x y z); i=7; echo "${a[i % ${#a[@]} ? i % ${#a[@]} : 2]}"
